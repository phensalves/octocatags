<%= form_with(model: github_user, local: true) do |form| %>
  <% if github_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(github_user.errors.count, "error") %> prohibited this github_user from being saved:</h2>

      <ul>
      <% github_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :login %>
    <%= form.text_field :login %>
  </div>

  <div class="field">
    <%= form.label :github_id %>
    <%= form.number_field :github_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
